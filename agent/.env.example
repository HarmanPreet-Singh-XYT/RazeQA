# Fernet key encrypting the credential store at rest (agent/src/agent/credentials/store.py).
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CREDENTIAL_STORE_KEY=

# Anthropic API key for diff/intent analysis and journey planning agents.
ANTHROPIC_API_KEY=
