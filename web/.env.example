# Seeded test account for the sandbox's automated login step (Section 3.3 of idea.md).
# Never a real/prod account — the platform injects these into the Docker sandbox as
# env vars at container boot, never into the repo or plaintext logs.
TEST_USER_EMAIL=qa@example.com
TEST_USER_PASSWORD=changeme123
